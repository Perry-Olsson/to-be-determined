PORT
SECRET
DATABASE_URL -- postgres://postgres:secret@localhost:5432/dev for docker container 
CONFIRMATION_SECRET
SMTP_LOGIN -- only in prod
SMTP_PASSWORD -- only in prod
CONFIRMATION_URL -- http://localhost:$PORT/user/confirm for development